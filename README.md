# Daily Tabs OS

Browser productivity workspace built around tab and session management.

## Structure

- `src/` — application source
- `extension/` — browser-extension components

## Purpose

The project explores a lightweight browser workflow for organizing frequently used tabs and daily working sessions.

## Status

Experimental / active development workspace.

Implementation details should be taken from the current source tree and extension configuration.
